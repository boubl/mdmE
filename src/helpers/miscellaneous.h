#include <string>

namespace helpers {
	namespace miscellaneous {
		std::string intToSomeDigit(int n, int len);
		void replaceAll(std::string& str, const std::string& from, const std::string& to);
	}
}